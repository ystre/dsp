# TODO: Use Helm tests. This is the original file generated by `helm create`.
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dsp-svc.fullname" . }}-test-connection"
  labels:
    {{- include "dsp-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dsp-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
